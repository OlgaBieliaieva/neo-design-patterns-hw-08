/**
 * Component у патерні Composite.
 *
 * Спільний інтерфейс для всіх вузлів документа.
 * Завдяки цьому клієнт працює однаково
 * як з простими елементами, так і з секціями.
 */
export interface DocNode {
  render(): string;
} 